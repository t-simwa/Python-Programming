
# Create a function `combine_lists` that takes two lists, unpacks them, and combines them into a single list.

def combine_lists(list1, list2): 
    return list1 + list2

list1 = [1, 2, 3]
list2 = [4, 5, 6]
print(combine_lists(list1, list2))   # Output: [1, 2, 3, 4, 5, 6]

