
# Python files with a `.pyc` or `.pyo` extension are compiled bytecode files. 
# These files are generated to improve performance by skipping the parsing step when the module is imported.

# Run the following command to see the compiled file generated:
# python -m py_compile example.py